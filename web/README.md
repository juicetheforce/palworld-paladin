# Paladin frontend

React + TypeScript, dark mode default (§5.1). Built bundle is embedded
into the Go binary via go:embed. UI design target: simple, single-screen,
everything at a glance (§4a reference map).
