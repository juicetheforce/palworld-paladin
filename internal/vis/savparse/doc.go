// Package savparse — save-file access behind a single interface, so linking PST's parser and shelling out to a sav_cli sidecar are interchangeable implementations (open item, §11).
//
// Design reference: docs/DESIGN.md §5.1, §6.5
package savparse
