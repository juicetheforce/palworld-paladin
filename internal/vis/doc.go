// Package vis — the two-tier visibility layer: live tier polling /v1/api/game-data (never persisted wholesale) and the historical tier via save parsing.
//
// Design reference: docs/DESIGN.md §6.5
package vis
